# Recalibration Questionnaire

Hosted fillable pre-call recalibration questionnaire for Lawrence Elliot coaching clients.
Self-contained single-page form. Answers are saved in the client's own browser
(localStorage) and never leave their device until they tap **Generate message** and
send the compiled text back manually. No data is collected server-side.
